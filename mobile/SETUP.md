# Drivora — Project Setup

## Tech Stack

| Tool                           | Version  | Purpose                            |
| ------------------------------ | -------- | ---------------------------------- |
| Expo                           | ~54.0.34 | React Native framework             |
| Expo Router                    | ~6.0.23  | File-based navigation              |
| NativeWind                     | ^4.2.6   | Tailwind CSS for React Native      |
| React Native Reanimated        | ~4.1.1   | Animations                         |
| React Native Safe Area Context | ~5.6.0   | Safe area handling                 |
| Tailwind CSS                   | ^3.4.19  | Utility-first CSS (via NativeWind) |
| TypeScript                     | ~5.9.2   | Type safety                        |

---

## Initial Setup Steps

### 1. Create Expo App

```bash
npx create-expo-app@latest ./
```

Bootstraps an Expo + Expo Router project with TypeScript, file-based routing, and the default tab layout.

---

### 2. Install NativeWind & Dependencies

```bash
npm install nativewind react-native-reanimated react-native-safe-area-context
npm install --save-dev tailwindcss@^3.4.17 prettier-plugin-tailwindcss@^0.5.11 babel-preset-expo
```

---

### 3. Initialize Tailwind Config

```bash
npx tailwindcss init
```

---

## Configuration Files

### `tailwind.config.js`

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./app/**/*.{js,jsx,ts,tsx}", "./components/**/*.{js,jsx,ts,tsx}"],
  presets: [require("nativewind/preset")],
  darkMode: "media",
  plugins: [],
};
```

- `nativewind/preset` enables the NativeWind utility set.
- `content` paths cover all component and screen files.
- `darkMode: "media"` uses system preference for dark mode.

---

### `global.css`

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Entry CSS file consumed by Metro via NativeWind.

---

### `metro.config.js`

```js
const { getDefaultConfig } = require("expo/metro-config");
const { withNativeWind } = require("nativewind/metro");

const config = getDefaultConfig(__dirname);

module.exports = withNativeWind(config, { input: "./global.css" });
```

Wraps the Metro config with NativeWind, pointing to `global.css` as the Tailwind input.

---

### `babel.config.js`

```js
module.exports = function (api) {
  api.cache(true);
  return {
    presets: [
      ["babel-preset-expo", { jsxImportSource: "nativewind" }],
      "nativewind/babel",
    ],
  };
};
```

- `jsxImportSource: "nativewind"` enables the `className` prop on all React Native components.
- `"nativewind/babel"` transforms Tailwind class names at build time.

---

### `nativewind-env.d.ts`

```ts
/// <reference types="nativewind/types" />
```

Auto-generated by NativeWind. Provides TypeScript types for `className` on React Native components. **Do not edit manually.**

---

### `tsconfig.json`

```json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "nativewind-env.d.ts"
  ]
}
```

- Path alias `@/` maps to the project root for clean imports.
- Includes `nativewind-env.d.ts` for className type support.

---

### `app.json` highlights

```json
{
  "expo": {
    "newArchEnabled": true,
    "android": { "edgeToEdgeEnabled": true },
    "experiments": {
      "typedRoutes": true,
      "reactCompiler": true
    }
  }
}
```

- **New Architecture** enabled for improved performance.
- **Typed Routes** for type-safe `expo-router` navigation.
- **React Compiler** experiment enabled.

---

## Running the App

```bash
# Start dev server (Expo Go)
npm start

# iOS simulator
npm run ios

# Android emulator
npm run android

# Web
npm run web
```

---

## Using NativeWind

Use the `className` prop on any React Native component:

```tsx
import { View, Text } from "react-native";

export default function Example() {
  return (
    <View className="flex-1 items-center justify-center bg-white dark:bg-black">
      <Text className="text-xl font-bold text-gray-900 dark:text-white">
        Hello Drivora
      </Text>
    </View>
  );
}
```

---

## Path Aliases

Use `@/` to reference files from the project root:

```ts
import { MyComponent } from "@/components/MyComponent";
import { useTheme } from "@/hooks/useTheme";
```

---

## Notes

- `prettier-plugin-tailwindcss` is installed to auto-sort Tailwind class names in Prettier.
- All Babel `@babel/plugin-proposal-*` plugins use `loose: true` for compatibility with Reanimated's worklet system.
- `react-native-worklets` is included as a dependency of Reanimated 4.x (shared value internals).

---
